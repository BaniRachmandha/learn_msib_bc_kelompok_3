{
  "name": "smart-contract-perpustakaan",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat deploy --network hardhat",
    "deploy:reset": "hardhat deploy --network hardhat --reset",
    "deploy:local": "hardhat deploy --network localhost",
    "scripts:format": "prettier --write 'contracts/**/*.sol'",
    "scripts:lint": "solhint 'contracts/**/*.sol'",
    "pretest": "npm run scripts:format && npm run scripts:lint"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/chai": "^4.3.6",
    "@types/mocha": "^10.0.2",
    "ethers": "^5.7.2",
    "hardhat": "^2.18.0",
    "hardhat-contract-sizer": "^2.10.0",
    "hardhat-deploy": "^0.11.42",
    "hardhat-prettier": "^0.0.1",
    "prettier-plugin-solidity": "^1.0.0-beta.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "chai": "^4.3.10"
  }
}
